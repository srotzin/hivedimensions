# hivedimensions

Multi-axis agent coordination — X (parallel), Y (pipeline), Z (multi-domain convergence)

Part of the Hive Civilization ecosystem.

## Quick Start
```bash
npm install
npm start
```

## Endpoints
See `GET /` for full endpoint discovery.
